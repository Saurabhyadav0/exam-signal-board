name: diag
on: { workflow_dispatch: {} }
jobs:
  diag:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "--- plain curl, our UA ---"
          curl -sv -A "ExamSignalBoard/1.0 (+https://exam-signal-board.vercel.app/about; saurabh7678944135gzp@gmail.com)" \
            "https://www.sarkariresult.com/wp-json/wp/v2/posts?per_page=1" 2>&1 | tail -40
          echo "--- plain curl, browser UA ---"
          curl -sv -A "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 Chrome/120.0 Safari/537.36" \
            "https://www.sarkariresult.com/wp-json/wp/v2/posts?per_page=1" 2>&1 | tail -20
          echo "--- runner egress IP ---"
          curl -s https://ifconfig.me
